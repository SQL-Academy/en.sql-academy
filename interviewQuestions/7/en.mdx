---
meta:
    title: 'How to remove duplicates in the result of an SQL query?'
    description: 'Removing duplicates in SQL is done using the DISTINCT keyword. Learn how to use it to retrieve unique records.'
---

Use the `DISTINCT` keyword in the `SELECT` statement to return only unique records.

Пример:

```sql
SELECT DISTINCT position
FROM employees;
```
