---
meta:
    title: 'What is SQL and What is it Used For?'
    description: 'SQL is a language for working with relational databases that allows creating tables, managing data, and performing key operations with it.'
---

> SQL (Structured Query Language) is the standard language for interacting with relational databases.
> It is used to define, manage, and retrieve data from databases.
> With SQL, you can perform the following operations:

-   Create new databases and tables.
-   Insert new data into tables.
-   Read data using queries.
-   Update existing data.
-   Delete data.
-   Manage access and permissions.
